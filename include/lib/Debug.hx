package lib;

class Debug {
  // To enable fast text scrolling:
  public static inline var FAST_WRITING:Bool = false;
  
  // To enable commands in the console (also see Main.hx)
  public static inline var CONSOLE_COMMANDS:Bool = false;
  
  // To enable scenario hot-loading
  public static inline var REMOTE_SCENARIO:Bool = false;
  
  // To disable music playback
  public static inline var DISABLE_MUSIC:Bool = false;
}
